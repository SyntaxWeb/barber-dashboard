export type GalleryUpload = { id: string; file: File; preview: string };
export type ExistingGalleryPhoto = { url: string; path: string | null };
export type WhatsappInfo = { phone?: string | null; pushname?: string | null };
